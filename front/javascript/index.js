fetch ("http://127.0.0.1:3000/api/products")
    .then(Json => Json.json())

    function AllKanape (Json){
        var nom = Json['name'];
        for (var i=0; i<Json.length; i++){
            
        }
    }